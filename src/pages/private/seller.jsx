import Footer from "components/Footer";

const Seller= ({children}) => {
    return (
        <div>
            <h2>This is the admin Page for sellers</h2>
            {children}
        </div>
    )
}

export default Seller;
